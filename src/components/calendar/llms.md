# Calendar
`shadcn-calendar` is an accessible single-date calendar. Use ISO `value`, `month`, `min`, and `max` strings. Listen for `calendar-change` and `calendar-month-change`.

```html
<shadcn-calendar value="2026-06-21" min="2026-01-01"></shadcn-calendar>
```
