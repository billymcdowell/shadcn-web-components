# Date Picker
`shadcn-date-picker` composes a shadcn-style trigger with `shadcn-calendar`. It supports `value`, `name`, `placeholder`, `locale`, `min`, `max`, `required`, `disabled`, `readonly`, and `open`.

```html
<shadcn-date-picker name="startDate" required></shadcn-date-picker>
```
